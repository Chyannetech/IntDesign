<%= form_with model: [ @product, @product.comments.build ] do |form| %>
    <p>
        <%= form.label :name %><br>
        <%= form.text_field :commenter %>
    </p>
    <p>
    <%= form.label :review %><br>
    <%= form.text_area :body %>
    </p>
    <p>
    <%= form.submit %>
    </p>
    <% end %>